name: Build Binaries

on:
  pull_request:
    types:
      - opened
      - synchronize
  # push:
    # tags:
    #   - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@main

      - name: Build Binary Linux x64
        uses: make linux-x64
